<!DOCTYPE html>
<html>
  <head>
    <title>Flashcards</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class='navbar navbar-inverse'>
      <div class='container'>
        <div class='navbar-header'>
          <%= link_to 'Flashcards', root_path, class: 'navbar-brand' %>
        </div>
        <div id='navbar'>
          <ul class='nav navbar-nav'>
            <% if current_user %>
              <li><%= link_to 'All cards', cards_path(@cards) %></li>
              <li><%= link_to 'Add a card', new_card_path %></li>
            <% else %>
              <li><%= link_to 'Sign Up', sign_up_path %></li>
              <li><%= link_to 'Log In', log_in_path %></li>
              <li><%= link_to 'Login with Twitter', auth_at_provider_path(provider: :twitter) %></li>
              <li><%= link_to 'Login with Github', auth_at_provider_path(provider: :github) %></li>
              <li><%= link_to 'Login with VK', auth_at_provider_path(provider: :vk) %></li>
              <li><%= link_to 'Login with Facebook', auth_at_provider_path(provider: :facebook) %></li>
            <% end %>
          </ul>
          <% if current_user %>
            <ul class='nav navbar-nav pull-right'>
              <li><%= link_to 'Edit Profile', edit_user_path(current_user.id) %></li>
              <li><%= link_to 'Log Out', log_out_path, method: :delete %></li>
            </ul>
          <% end %>
        </div>
      </div>
    </nav>

    <div class='container'>
      <div class='col-md-12 col-sm-offset-3' style='font-size: 36px;'>
        Learn English with flashcards easily!
      </div><br><br>
    </div>
    <br>
    <div class='container'>
      <% flash.each do |key, value| %>
        <div class='alert alert-<%= key %> text-center'>
          <%= value %>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
