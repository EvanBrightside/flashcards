<div class='col-md-4 col-md-offset-4 text-center panel panel-default'>
  <h1>Update your profile</h1>

  <%= simple_form_for @user do |f| %>
    <%= render partial: 'form', locals: {f: f} %>
    <br>
    <%= f.button :submit, 'Save', class: 'btn btn-success btn-block' %>
    <br>
  <% end %>

</div>
